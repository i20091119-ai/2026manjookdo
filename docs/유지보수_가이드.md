# 경남수학문화관 만족도 대시보드 — 유지보수 가이드

최종 수정: 2026-08-05 (월말 집계 탭 추가 · 캠프 연동)

---

## 목차

1. [전체 구조 이해하기](#1-전체-구조-이해하기)
2. [배포 방법 (가장 중요)](#2-배포-방법-가장-중요)
3. [월말 집계 탭](#3-월말-집계-탭)
4. [상황별 수정 방법](#4-상황별-수정-방법)
5. [문제 해결 (트러블슈팅)](#5-문제-해결-트러블슈팅)
6. [파일별 상세 위치 색인](#6-파일별-상세-위치-색인)

---

## 1. 전체 구조 이해하기

### 파일 구성

Apps Script 프로젝트에는 **2개 파일**이 있습니다.

| 파일명 | 종류 | 역할 |
|---|---|---|
| `Code.gs` | 스크립트 | 스프레드시트에서 데이터를 읽어옴 (서버) |
| `index.html` | HTML | 화면 표시, 통계 계산 (클라이언트) |

> 예전에는 `logo.html` / `scene.html`에 이미지 base64를 넣었지만, 지금은
> **구글 드라이브 파일에서 직접 읽어옵니다.** 두 파일은 더 이상 쓰지 않습니다.

### 동작 흐름

```
사용자가 웹앱 URL 접속
   ↓
비밀번호 입력 (3141)
   ↓
index.html이 Code.gs의 getAllData()를 한 번 호출
   ↓
getAllData()가 4개 시트 + 로고/전경 이미지를 한꺼번에 읽어서 반환
   ├── 자유관람
   ├── 교구대여
   ├── 학교·가족
   └── 캠프
   ↓
받아온 데이터를 index.html이 계산해서 표로 표시
```

**핵심 원칙**: `Code.gs`는 **데이터를 가져오기만** 하고,
통계 계산과 화면 표시는 전부 `index.html`이 합니다.

### 현재 탭 구성 (12개)

| 번호 | 탭 이름 | 데이터 출처 | 필터 기준 |
|---|---|---|---|
| 0 | 📊 월말 집계 | 4개 시트 전부 | — |
| 1 | 자유관람 | 자유관람 시트 | 없음 |
| 2 | 교구대여(수학) | 교구대여 시트 | B열 = `수학교구` |
| 3 | 교구대여(SW) | 교구대여 시트 | B열 = `SW교구` |
| 4 | 학교·가족(학교수학) | 학교·가족 시트 | B열 = `학교수학체험프로그램` |
| 5 | 학교·가족(학교SW) | 학교·가족 시트 | B열 = `학교SW체험프로그램` |
| 6 | 학교·가족(가족수학) | 학교·가족 시트 | B열 = `가족수학체험프로그램` |
| 7 | 학교·가족(가족SW) | 학교·가족 시트 | B열 = `가족SW체험프로그램` |
| 8 | 캠프(초등수학) | 캠프 시트 | B열 = `초등학생 체험수학캠프` |
| 9 | 캠프(초등SW) | 캠프 시트 | B열 = `초등학생 SW캠프` |
| 10 | 캠프(중등수학) | 캠프 시트 | B열 = `중학생 체험수학캠프` |
| 11 | 캠프(고등AI수학) | 캠프 시트 | B열 = `고등학생 인공지능수학캠프` |

---

## 2. 배포 방법 (가장 중요)

### 코드를 수정한 뒤 반드시 해야 하는 일

수정만 하고 저장하면 **웹앱에 반영되지 않습니다.** 반드시 재배포해야 합니다.

1. Apps Script 편집기에서 코드 수정
2. 상단 **저장** 버튼 클릭 (또는 `Ctrl+S`)
3. 우측 상단 **배포** 버튼 클릭
4. **배포 관리** 선택
5. 기존 배포 항목 오른쪽의 **연필(수정)** 아이콘 클릭
6. **버전** 드롭다운에서 **새 버전** 선택
7. **배포** 버튼 클릭

> ⚠️ **주의**: "새 배포"를 하면 URL이 바뀝니다. 기존 URL을 유지하려면 반드시
> **배포 관리 → 수정 → 새 버전**으로 하세요.

### 반영이 안 될 때

브라우저 캐시 때문일 수 있습니다.

- `Ctrl + Shift + R` (강력 새로고침)
- 또는 시크릿 창에서 열기

---

## 3. 월말 집계 탭

### 무엇을 하는 탭인가

선택한 연도·월의 보고서용 표 4개를 자동으로 만들어 줍니다.
각 표 우측의 **📋 복사** 버튼을 누르면 데이터가 클립보드에 복사되고,
한컴/엑셀 양식의 **첫 번째 데이터 셀**에 `Ctrl+V` 하면 그대로 들어갑니다.

> 복사되는 것은 **머리글과 합계 줄을 뺀 데이터 행만**입니다.
> 양식에 이미 머리글과 합계 수식이 있는 것을 전제로 합니다.

| 표 | 내용 | 사람 분류 |
|---|---|---|
| 1. 운영 현황 | 총괄 집계 양식 | 7분류: 유 / 초 / 중 / 고 / 교원 / 보호자 / 일반 |
| 2. 참여자 현황 | 거제 양식 | 6분류: 유아·일반을 '그 외'로 통합 |
| 3. 만족도 통계 | 문항1~5 × 5단계 | — |
| 4. 만족도 결과 | 만족도(%) | — |

**만족도(%) 계산식**

```
만족도(%) = [ '만족' 이상 답변 수의 합 / (설문 응답자 수 × 설문 문항 수) ] × 100
```

- 프로그램 운영 만족도 = 문항1~4 (문항 수 4)
- 강사 만족도 = 문항5 (문항 수 1)

### 프로그램 행을 고치려면

`index.html`의 **`REPORT_PROGRAMS`** 배열 하나만 고치면 4개 표에 모두 반영됩니다.
(834번째 줄 근처)

```javascript
{ grp:0, no:'1-(08)', name:'가족과 함께하는 수학체험교실',
  src:'prog',           // 어느 시트: 'free' | 'educ' | 'prog' | 'camp' | null
  field:'programType',  // 무엇으로 거를지
  filter:'가족수학체험프로그램',   // 시트 B열 값과 정확히 일치해야 함
  hasInstructor:true }, // 문항5(강사 만족도)가 있는 설문인지
```

- **`src:null`** 로 두면 데이터 원천이 없는 행이 되어 **회색 양식으로만** 표시됩니다.
  (경남학생수학탐구페스티벌, 기타, 직무연수가 여기에 해당)
- **`filter`에 배열**을 주면 여러 설문 종류를 한 행으로 합산합니다.

### 캠프가 어떻게 집계되는가

캠프 설문에는 응답자 구분 컬럼이 없습니다.
그래서 **캠프 종류명에 들어 있는 학교급**으로 분류합니다.

| 캠프 종류 | 분류 | 들어가는 행 |
|---|---|---|
| 초등학생 체험수학캠프 | 초 | 1-(03) 학생체험수학캠프 |
| 중학생 체험수학캠프 | 중 | 1-(03) 학생체험수학캠프 |
| 고등학생 인공지능수학캠프 | 고 | 1-(03) 학생체험수학캠프 |
| 초등학생 SW캠프 | 초 | 1-(04) 학생SW체험캠프 |

캠프 종류가 새로 생기면 두 곳을 함께 고칩니다.

1. `TABS` 배열 — 상세 탭을 추가하려면
2. `REPORT_PROGRAMS`의 `1-(03)` 또는 `1-(04)` 행 `filter` 배열 — 월말 집계에 넣으려면

> 종류명에 '초등/중학/고등'이 없으면 '일반'으로 분류됩니다.
> 종류명 규칙이 바뀌면 `schoolLevelOfCamp()` 함수를 고쳐야 합니다.

### 사람 분류 규칙

`classifyPerson()` 함수가 응답자 텍스트를 7분류로 바꿉니다.
**위에서부터 순서대로 검사**하므로 순서가 중요합니다.

```javascript
'학부모' 또는 '보호자' 포함  → 보호자     ← 반드시 맨 위
'교원'/'교사'/'교직원' 포함  → 교원
'유아' 포함                  → 유
'초등' 포함                  → 초
'중학' 포함                  → 중
'고등' 포함                  → 고
'학생' 포함 (구버전 단독 표기) → 초
그 외                        → 일반
```

`'초등학생 학부모'`가 `'보호자'`로 가는 이유가 이 순서 때문입니다.
학부모 검사를 아래로 내리면 학부모가 학생으로 집계되는 버그가 생깁니다.

---

## 4. 상황별 수정 방법

### 4-1. 비밀번호를 바꾸고 싶을 때

**파일**: `index.html` · **위치**: 197번째 줄 근처

```javascript
const CORRECT_PW   = '3141';
```

`'3141'` 부분을 원하는 4자리 숫자로 바꾸면 됩니다.

> 4자리가 아닌 비밀번호를 쓰려면 `maxlength="4"`(HTML 입력칸)와 점 4개 표시 부분도
> 함께 수정해야 하므로, **4자리 유지를 권장**합니다.

> ⚠️ 이 비밀번호는 화면을 가리는 용도이지 실제 보안이 아닙니다.
> URL을 아는 사람은 우회할 수 있습니다. (5번 절 마지막 참고)

---

### 4-2. 설문 문항 내용이 바뀌었을 때

**파일**: `index.html` · **위치**: 227~272번째 줄

| 상수명 | 줄 번호 | 해당 설문 |
|---|---|---|
| `FREE_ITEMS` | 227 | 자유관람 (8문항) |
| `EDUC_ITEMS_STUDENT` | 238 | 교구대여 학생용 (4문항) |
| `EDUC_ITEMS_TEACHER` | 244 | 교구대여 교원용 (4문항) |
| `PROG_ITEMS` | 254 | 학교·가족 (9문항) |
| `CAMP_ITEMS` | 266 | 캠프 (5문항) |

**수정 예시** — 자유관람 프1 문항을 바꾸려면:

```javascript
const FREE_ITEMS = [
  {label:'프1. 여기를 원하는 문구로 수정', type:'prog'},   // ← 이 부분
  {label:'프2. 프로그램이 이해하기 쉽게 잘 구성되었나요?', type:'prog'},
  ...
];
```

> `type`은 색상 구분용입니다. `'prog'`는 파란색(프로그램 문항), `'fac'`는 초록색(시설 문항)입니다.

---

### 4-3. 문항 개수가 늘거나 줄었을 때

**두 파일을 모두 고쳐야 합니다.**

#### ① `Code.gs` — 읽어올 열 범위 수정

예를 들어 캠프 문항이 5개 → 6개로 늘었다면 (C~G에서 C~H로):

**위치**: `getCampData()` 함수 내부 (191번째 줄 근처)

```javascript
// 변경 전 — C열(2번)부터 5개
scores: toScores(row, 2, 5),

// 변경 후 — C열(2번)부터 6개
scores: toScores(row, 2, 6),
```

> **컬럼 번호는 0부터 시작합니다.**
> A=0, B=1, C=2, D=3, E=4, F=5, G=6, H=7, I=8, J=9, K=10, L=11, M=12, N=13, O=14, P=15

주관식 열 번호도 한 칸씩 밀리므로 함께 수정:

```javascript
comment1: cell(row, 8),   // 기존 7 → 8
comment2: cell(row, 9),   // 기존 8 → 9
```

#### ② `index.html` — 문항 목록에 추가

```javascript
const CAMP_ITEMS = [
  {label:'1. 참여한 프로그램 내용이 흥미로웠나요?', type:'prog'},
  ...
  {label:'6. 새로 추가된 문항', type:'prog'},   // ← 추가
];
```

> `Code.gs`의 `toScores(...)` 개수와 `index.html`의 문항 목록 개수가 **반드시 일치**해야 합니다.

#### ③ 월말 집계 영향 확인

월말 집계는 **문항1~4 = 프로그램 운영, 문항5 = 강사**라는 규칙에 기대고 있습니다.
문항 순서가 바뀌어 이 규칙이 깨지면 `Q_PROGRAM` / `Q_INSTR` 상수와
`itemStat()` 호출 부분을 함께 고쳐야 합니다.

#### ④ 테스트 실행

```bash
node test/report.test.js
```

집계가 틀어졌으면 여기서 잡힙니다.

---

### 4-4. 새로운 프로그램 종류가 생겼을 때

예: 캠프에 '중학생 SW캠프'가 추가된 경우.

#### ① 상세 탭 추가 — `index.html`의 `TABS` 배열 (210번째 줄)

```javascript
{ kind:'camp', label:'캠프 (중등SW)', src:'camp', field:'campType',
  filter:'중학생 SW캠프' },
```

> 탭 버튼 HTML은 이 배열에서 **자동 생성**됩니다. 버튼을 따로 추가할 필요가 없습니다.

#### ② 월말 집계에 반영 — `REPORT_PROGRAMS`의 해당 행 (834번째 줄)

```javascript
{ grp:0, no:'1-(04)', name:'학생SW체험캠프', src:'camp', field:'campType',
  filter:['초등학생 SW캠프','중학생 SW캠프'],   // ← 추가
  hasInstructor:true },
```

`filter` 문자열은 **시트 B열 값과 글자 하나까지 같아야 합니다.**
실제 값은 Apps Script 편집기에서 `diagnose()`를 실행해 로그로 확인하세요.

---

### 4-5. 새로운 설문(스프레드시트)을 추가하고 싶을 때

#### ① `Code.gs` 상단에 시트 ID 추가 (9~12번째 줄)

```javascript
const NEW_SS_ID = '여기에_스프레드시트_ID';
```

> 스프레드시트 ID는 URL에서 `/d/` 와 `/edit` 사이의 긴 문자열입니다.
> 예: `docs.google.com/spreadsheets/d/`**`1AbC2DeF3GhI`**`/edit`

#### ② `Code.gs`에 데이터 읽기 함수 추가

기존 `getCampData()`를 복사해서 이름과 열 번호만 바꾸면 됩니다.

```javascript
function getNewData() {
  const ss    = SpreadsheetApp.openById(NEW_SS_ID);
  const sheet = ss.getSheetByName('설문지 응답') || ss.getSheets()[0];
  const data  = sheet.getDataRange().getValues();
  const rows  = [];
  const yearSet = {};

  for (let i = 1; i < data.length; i++) {
    const row = data[i];
    if (!row[0]) continue;
    const d = parseTs(row[0]);
    if (!d) continue;
    yearSet[d.year] = true;
    rows.push({
      year: d.year, month: d.month, day: d.day,
      newType: cell(row, 1),
      scores: toScores(row, 2, 3),   // C열부터 3문항
      comment1: cell(row, 5),
    });
  }
  return { years: yearKeys(yearSet), rows: rows };
}
```

#### ③ `getAllData()`의 목록에 추가 (237번째 줄)

```javascript
const jobs = [
  ['free',   getFreeData],
  ['educ',   getEducData],
  ['prog',   getProgramData],
  ['camp',   getCampData],
  ['newdata', getNewData],     // ← 추가
  ['images', getImages],
];
```

#### ④ `index.html` 수정

**`SRC_KEYS`에 추가** (연도·월 목록이 새 시트도 보게 됨):

```javascript
const SRC_KEYS = ['free','educ','prog','camp','newdata'];
```

**`DATA` 초기값에 추가**:

```javascript
let DATA = { free:null, educ:null, prog:null, camp:null, newdata:null };
```

**`loadAll()`의 성공 핸들러에 한 줄 추가**:

```javascript
DATA.newdata = res.newdata || {years:[],rows:[]};
```

**`TABS`에 탭 추가**:

```javascript
{ kind:'newkind', label:'새 설문(종류A)', src:'newdata',
  field:'newType', filter:'종류A' },
```

**`render()`의 `switch`에 분기 추가**:

```javascript
case 'newkind': html = buildNew(year,month,tab); break;
```

**빌더 함수 추가** — `buildCamp()`를 복사해서 수정하면 됩니다.

---

### 4-6. 응답자 분류를 바꾸고 싶을 때

설문 보기 항목이 바뀌면 분류 매핑을 수정해야 합니다.
**분류 매핑이 세 군데 있으니 목적에 맞는 곳을 고치세요.**

| 함수/상수 | 줄 번호 | 어디에 쓰이나 |
|---|---|---|
| `FREE_PART_ENTRIES` | 277 | 자유관람 상세 탭 |
| `mapProgRespondent()` | 299 | 학교·가족 상세 탭 |
| `classifyPerson()` | 868 | **월말 집계 표 1·2** |

#### 자유관람 (상세 탭)

```javascript
const FREE_PART_ENTRIES = [
  ['학부모 등 보호자','학부모 등 보호자'],
  ['초등학생 학부모','학부모 등 보호자'],
  ...
];
```

- 앞의 값 = 시트에 실제로 들어오는 텍스트
- 뒤의 값 = 통계에 표시할 분류명

> ⚠️ **순서가 중요합니다.** `초등학생 학부모`가 `초등학생`보다 **위에** 있어야 합니다.
> 위에서부터 순서대로 검사하기 때문에, 짧은 키가 먼저 오면 잘못 매칭됩니다.

#### 학교·가족 프로그램 (상세 탭)

```javascript
function mapProgRespondent(v) {
  const s = (v||'').trim();
  if (s.includes('학부모') || s.includes('보호자')) return '학부모 등 보호자';
  if (s.includes('학생')) return '학생';
  if (s.includes('교원') || s.includes('교사') || s.includes('교직원')) return '교원';
  if (s === '') return null;
  return '그외';
}
```

#### 월말 집계 (표 1·2)

`classifyPerson()` — 3번 절의 '사람 분류 규칙' 참고.
새 표현이 들어오면 여기에도 조건을 추가해야 보고서 숫자가 맞습니다.

---

### 4-7. 만족도 응답 표현이 바뀌었을 때

**파일**: `Code.gs` · **위치**: 51번째 줄 `scoreToNum()` 함수

현재 다음 표현들을 인식합니다.

| 점수 | 인식하는 표현 |
|---|---|
| 5 | `매우 만족`, `매우만족`, `매우 그렇다`, `5` |
| 4 | `만족`, `그렇다`, `4` |
| 3 | `보통`, `보통이다`, `3` |
| 2 | `불만족`, `그렇지 않다`, `2` |
| 1 | `매우 불만족`, `매우불만족`, `전혀 그렇지 않다`, `1` |

괄호가 붙은 복합형(`매우 만족(매우 그렇다)`)과
구글 폼 '선형 배율'의 숫자(1~5)도 처리합니다.

새 표현이 생기면 해당 점수 줄에 조건을 추가하세요.

```javascript
if (s.indexOf('아주 좋음') === 0) return 5;   // 예시
```

> ⚠️ **검사 순서를 지키세요.** `'매우 불만족'`을 `'불만족'`보다,
> `'불만족'`을 `'만족'`보다 먼저 검사해야 합니다. 순서를 바꾸면
> 불만족 응답이 만족으로 집계됩니다.

수정 후 확인:

```bash
node test/score.test.js
```

---

### 4-8. 이미지를 교체하고 싶을 때

이미지는 **구글 드라이브 파일에서 직접 읽어옵니다.**

1. 새 이미지를 구글 드라이브에 올립니다.
2. 파일을 열고 URL에서 파일 ID를 복사합니다.
   `drive.google.com/file/d/`**`1AbC2DeF3GhI`**`/view`
3. `Code.gs` 상단(17~18번째 줄)의 ID를 바꿉니다.

```javascript
const LOGO_FILE_ID  = '1d4ApBhKXMX3Q0SnJmlBlJRQXIauUsUdH';
const SCENE_FILE_ID = '1Dq6GJOrSDYPr9w64VIQrGWZ5nkn7HkPw';
```

4. 재배포합니다.

> 웹앱을 **배포한 계정**이 그 드라이브 파일을 읽을 수 있어야 합니다.

---

### 4-9. 스프레드시트 시트 탭 이름이 바뀌었을 때

**파일**: `Code.gs` · **위치**: 13~14번째 줄

```javascript
const SHEET_NAME = '설문지 응답';
const CAMP_SHEET_NAME = '설문지 응답 시트1';
```

> 자유관람·교구대여·학교가족은 `SHEET_NAME`을 공통으로 씁니다.
> 셋 중 하나만 이름이 다르다면 해당 함수에서 개별 지정해야 합니다.
> 이름을 못 찾으면 첫 번째 시트를 자동으로 씁니다.

---

## 5. 문제 해결 (트러블슈팅)

### 가장 먼저 할 일 — `diagnose()` 실행

Apps Script 편집기에서 함수 선택 드롭다운에서 **`diagnose`** 를 고르고 **실행**하세요.
**보기 → 실행 로그**에 이런 내용이 나옵니다.

```
[camp] 24행 · 연도 [2026]
    campType 고유값: {"초등학생 체험수학캠프":10,"중학생 체험수학캠프":14}
    ⚠ 점수가 전부 null인 행 3개 — scoreToNum() 확인 필요
```

- **고유값** — `TABS`나 `REPORT_PROGRAMS`의 `filter` 문자열과 대조하세요.
- **점수 전부 null** — 만족도 표현을 인식 못 한 것입니다. (4-7 참고)

---

### 증상: 코드가 텍스트로 그대로 보임

**원인**: `index.html` 파일이 없거나 이름이 잘못됨

1. Apps Script 왼쪽 파일 목록에 `index.html`이 있는지 확인
2. 없으면 **+ → HTML** 로 추가 (파일명은 `index`, `.html`은 자동)
3. 재배포

---

### 증상: 화면이 하얗게 비어 있음

**원인**: JavaScript 오류

1. `F12` 키를 눌러 개발자 도구 열기
2. **Console** 탭에서 **빨간색** 오류 메시지 확인
3. 오류 메시지에 나온 줄 번호 주변 코드 확인

---

### 증상: 특정 탭에만 "해당 월의 응답이 없습니다"가 뜸

화면이 **그 달 시트에 실제로 들어온 값들을 함께 보여줍니다.**
탭의 `filter` 문자열과 다르면 `TABS` 배열의 `filter`를 시트 값에 맞추세요.

띄어쓰기 하나만 달라도 안 걸립니다. (`초등학생 SW캠프` vs `초등학생SW캠프`)

---

### 증상: 월말 집계의 특정 행만 비어 있음

| 어떻게 보이나 | 원인 |
|---|---|
| 행 전체가 **회색** | `REPORT_PROGRAMS`에 `src:null` — 원천이 없는 행 (정상) |
| 숫자가 **0**으로 나옴 | 설문은 연결되어 있는데 그 달 응답이 0건 (정상) |
| 흰 행인데 **빈칸** | `filter` 문자열이 시트 값과 안 맞음 → `diagnose()` 실행 |

---

### 증상: 강사 만족도 칸이 비어 있음

자유관람과 교구대여에는 강사 문항(문항5)이 없습니다. **정상입니다.**
`REPORT_PROGRAMS`의 `hasInstructor` 값으로 결정됩니다.

---

### 증상: 숫자가 전부 0으로 나옴

**원인 1**: 컬럼 번호(인덱스)가 틀림

`Code.gs`에서 해당 설문 함수의 `toScores(row, 시작열, 개수)`를 확인하세요.
스프레드시트 실제 열 위치와 맞아야 합니다.

**원인 2**: 만족도 표현을 인식 못 함

`diagnose()`가 "점수가 전부 null인 행"을 알려 줍니다. (4-7 참고)

**확인 방법** — 브라우저 콘솔(F12)에서:

```javascript
google.script.run.withSuccessHandler(r => console.log(r.camp.rows.slice(0,3))).getAllData()
```

결과에서 `scores` 배열이 `[null, null, ...]`이면 표현 인식 실패입니다.

---

### 증상: 특정 대상만 통계에 안 나옴

**원인**: 응답자 분류 매핑 누락 → 4-6 참고

**확인 방법** — 스프레드시트 빈 칸에:

```
=UNIQUE(C2:C)
```

실제로 어떤 값들이 들어오는지 확인한 후, 없는 값을 매핑에 추가합니다.

---

### 증상: 복사 버튼을 눌러도 붙여넣기가 안 됨

브라우저가 클립보드 접근을 막은 경우입니다.
버튼이 "✅ 복사됨"으로 바뀌었는지 확인하고, 안 바뀌었다면
표를 마우스로 드래그해 직접 복사하세요.

---

### 증상: 이미지가 안 보임

1. `Code.gs`의 `LOGO_FILE_ID` / `SCENE_FILE_ID`가 맞는지 확인
2. 배포 계정이 그 드라이브 파일을 읽을 수 있는지 확인
3. Apps Script에서 **실행 → getImages** 를 눌러 오류가 없는지 확인

---

### 증상: 새 데이터가 반영 안 됨

1. 웹앱 화면의 **↻ 새로고침** 버튼 클릭
2. 그래도 안 되면 `Ctrl + Shift + R`
3. 그래도 안 되면 재배포

---

### 증상: 권한 오류가 뜸

Apps Script 편집기에서 아무 함수나 **실행** 버튼으로 한 번 돌려보세요.
권한 승인 팝업이 뜨면 승인하면 됩니다.

---

### ⚠️ 알아 두어야 할 보안 제약

이 웹앱은 **링크가 있는 누구나** 접근할 수 있게 배포되어 있습니다
(`appsscript.json`의 `ANYONE_ANONYMOUS`).

비밀번호(`3141`)는 화면을 가리는 용도일 뿐입니다.
URL을 아는 사람은 브라우저 개발자 도구로 비밀번호를 그대로 볼 수 있고,
아예 게이트를 건너뛰고 서버 함수를 직접 호출해 **주관식 응답 원문까지** 받을 수 있습니다.

실제로 접근을 막아야 한다면 둘 중 하나가 필요합니다.

1. `appsscript.json`의 `"access"`를 `"DOMAIN"`으로 바꿔 교육청 계정만 열기
   (외부 공유가 필요하면 못 씀)
2. 비밀번호 검증을 `Code.gs` 서버 쪽으로 옮기기 (코드 수정 필요)

지금 상태로 계속 쓸 거라면, **URL을 대외 공개 문서에 싣지 않는 것**만은 지켜 주세요.

---

## 6. 파일별 상세 위치 색인

### Code.gs (총 279줄)

| 줄 번호 | 내용 | 수정이 필요한 경우 |
|---|---|---|
| 9~12 | 스프레드시트 ID 4개 | 시트를 새로 만들거나 교체할 때 |
| 13~14 | 시트 탭 이름 | 하단 탭 이름이 바뀌었을 때 |
| 17~18 | 이미지 드라이브 파일 ID | 로고·전경 교체 시 |
| 20~24 | `doGet()` | 웹앱 제목 변경 시 |
| 30~37 | `parseTs()` | 날짜 형식이 특이할 때 |
| 51~80 | `scoreToNum()` | 만족도 표현이 바뀌었을 때 |
| 83~101 | `getSheet()` / `toScores()` / `cell()` / `yearKeys()` | — |
| 104~129 | `getFreeData()` | 자유관람 시트 열 구조 변경 시 |
| 131~158 | `getEducData()` | 교구대여 시트 열 구조 변경 시 |
| 160~189 | `getProgramData()` | 학교·가족 시트 열 구조 변경 시 |
| 191~217 | `getCampData()` | 캠프 시트 열 구조 변경 시 |
| 219~234 | `getImages()` | 이미지 로딩 방식 변경 시 |
| 237~260 | `getAllData()` | 설문 추가 시 |
| 263~ | `diagnose()` | 진단용 — 고칠 일 없음 |

### index.html (총 1208줄)

| 줄 번호 | 내용 | 수정이 필요한 경우 |
|---|---|---|
| ~180 | CSS 스타일 | 색상, 글꼴, 여백 조정 |
| 197 | `CORRECT_PW` | 비밀번호 변경 |
| 210~223 | `TABS` 배열 | **탭 추가·삭제, 필터값 변경** (버튼은 자동 생성) |
| 227~236 | `FREE_ITEMS` | 자유관람 문항 변경 |
| 238~252 | `EDUC_ITEMS_*` | 교구대여 문항 변경 |
| 254~264 | `PROG_ITEMS` | 학교·가족 문항 변경 |
| 266~272 | `CAMP_ITEMS` | 캠프 문항 변경 |
| 277~296 | `FREE_PART_*` | 자유관람 응답자 분류 |
| 299~306 | `mapProgRespondent()` | 학교·가족 응답자 분류 |
| ~380 | 게이트(비밀번호) 로직 | 로그인 화면 동작 |
| ~400 | `loadAll()` 데이터 로드 | 설문 추가 시 |
| ~450 | 연도·월 선택 | 필터 방식 변경 |
| ~490 | 탭 바 생성·렌더 분기 | 탭 종류 추가 시 |
| ~530 | `emptyFor()` 진단 메시지 | — |
| ~650 | 공통 표 생성 함수 | 표 형식 변경 |
| 660~688 | `buildFree()` | 자유관람 화면 구성 |
| 690~741 | `buildEduc()` | 교구대여 화면 구성 |
| 743~782 | `buildProg()` | 학교·가족 화면 구성 |
| 784~806 | `buildCamp()` | 캠프 화면 구성 |
| 810~820 | `CATS7` / `CATS6` | **월말 집계 분류 항목** |
| 834~851 | `REPORT_PROGRAMS` | **월말 집계 프로그램 행** |
| 856~862 | `schoolLevelOfCamp()` | 캠프 학교급 판별 |
| 868~880 | `classifyPerson()` | **월말 집계 사람 분류** |
| 922~ | `buildReport()` 및 표 1~4 | 월말 집계 표 구성 |
| ~1180 | `copyRep()` | 복사 동작 |

---

## 부록: 컬럼 번호 빠른 참조표

| 열 | 번호 | 열 | 번호 | 열 | 번호 |
|---|---|---|---|---|---|
| A | 0 | I | 8 | Q | 16 |
| B | 1 | J | 9 | R | 17 |
| C | 2 | K | 10 | S | 18 |
| D | 3 | L | 11 | T | 19 |
| E | 4 | M | 12 | U | 20 |
| F | 5 | N | 13 | V | 21 |
| G | 6 | O | 14 | W | 22 |
| H | 7 | P | 15 | X | 23 |

---

## 부록: 현재 연결된 스프레드시트

| 설문 | 시트 탭 이름 | 주요 컬럼 |
|---|---|---|
| 자유관람 | 설문지 응답 | B=참여자, E~L=문항8개, O=주1, P=주2 |
| 교구대여 | 설문지 응답 | B=교구종류, C=학교급, D=설문자, E~H=문항4개, I=주관식 |
| 학교·가족 | 설문지 응답 | B=프로그램종류, C=설문자, D~H=프1~5, I~L=시1~4, M~O=주1~3 |
| 캠프 | 설문지 응답 시트1 | B=캠프종류, C~G=문항5개, H=주1, I=주2 |

---

## 백업 권장 사항

코드를 수정하기 전에는 항상 백업을 남기세요.

**Apps Script 자체 버전 관리**
**배포 → 배포 관리** 에서 이전 버전으로 되돌릴 수 있습니다.

**깃 저장소**
이 프로젝트는 깃으로도 관리됩니다. 수정 전 상태로 언제든 되돌릴 수 있습니다.

**수동 백업**
Apps Script 편집기에서 코드 전체 선택(`Ctrl+A`) → 복사 → 텍스트 파일로 저장
